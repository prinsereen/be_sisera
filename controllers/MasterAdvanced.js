import MasterAdvanced from "../models/MasterDataAdvanced";
import Tahapan from "../models/TahapanModel";
import Video from "../models/VideoModel";
import CeritaCard from "../models/CeritaCardModel";

export const createMasterAdvaced = async(req, res) => {
    
    const {level, jenis, assets_1, assets_2, assets_3, } 

    try {
        
    } catch (error) {
        
    }
} 